// SignInScreen.ios.js

import React from 'react';
import SignInScreen from '../../screens/SignInScreen';

const SignInScreenIOS = (props) => {
  // Add iOS-specific modifications or imports here
  // For example, you can add iOS-specific styles, components, or logic
  return <SignInScreen {...props} />;
};

export default SignInScreenIOS;
